<!DOCTYPE hibernate-configuration PUBLIC
        "-//Hibernate/Hibernate Configuration DTD 3.0//EN"
        "http://www.hibernate.org/dtd/hibernate-configuration-3.0.dtd">

<hibernate-configuration>
    <session-factory>
        <!-- DB Properties -->
        <property name="hibernate.connection.driver_class">com.mysql.cj.jdbc.Driver</property>
        
        <!-- MySQL connection, adjusted for required 'fsadexam' DB -->
        <!-- Added createDatabaseIfNotExist=true so it doesn't fail if the user hasn't explicitly created it -->
        <property name="hibernate.connection.url">jdbc:mysql://localhost:3306/fsadexam?createDatabaseIfNotExist=true</property>
        
        <property name="hibernate.connection.username">root</property>
        <property name="hibernate.connection.password">Kl2436788@</property>

        <!-- Hibernate Properties -->
        <property name="hibernate.dialect">org.hibernate.dialect.MySQL8Dialect</property>
        <property name="hibernate.hbm2ddl.auto">update</property>
        <property name="hibernate.show_sql">true</property>

        <!-- Mapping Class Information -->
        <mapping class="com.klef.fsad.exam.Payment"/>
    </session-factory>
</hibernate-configuration>
